{
  "name": "proposal",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python -m http.server 5173 --bind 127.0.0.1",
    "start:phone": "python -m http.server 5173 --bind 0.0.0.0"
  }
}
